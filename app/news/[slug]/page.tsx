import type{Metadata}from'next';import Link from'next/link';import{notFound}from'next/navigation';import{ArrowLeft}from'lucide-react';import{SiteHeader}from'@/components/site/site-header';import{SiteFooter}from'@/components/site/site-footer';import{articles}from'@/lib/content';
export async function generateStaticParams(){return articles.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=articles.find(x=>x.slug===slug);return{title:a?.title??'News',description:a?.summary}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <main><SiteHeader/><article className="editorial shell"><Link className="back-link" href="/news"><ArrowLeft size={15}/> All dispatches</Link><p className="eyebrow">{a.category} · {a.date}</p><h1>{a.title}</h1><p className="editorial-lead">{a.summary}</p><div className="editorial-rule"/><p>{a.body}</p><p>Further details remain sealed while the relevant witnesses decide which version of events they remember.</p><footer>Filed by <strong>The Shadowverse Archive</strong></footer></article><SiteFooter/></main>}
