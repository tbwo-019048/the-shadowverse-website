'use client';
import Link from'next/link';import{useState}from'react';import{useRouter}from'next/navigation';import{LockKeyhole}from'lucide-react';import{getSupabase,supabaseConfigured}from'@/lib/supabase';import{Input}from'@/components/ui/input';import{Button}from'@/components/ui/button';import{Label}from'@/components/ui/label';
export default function AdminLogin(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const client=getSupabase();if(!client){setMessage('Connect Supabase in .env.local before signing in.');return}setBusy(true);const{error}=await client.auth.signInWithPassword({email,password});setBusy(false);if(error){setMessage(error.message);return}router.push('/admin')}return <main className="login-page"><Link className="wordmark" href="/"><span className="wordmark-symbol">S</span><span>The <strong>Shadowverse</strong></span></Link><form onSubmit={submit} className="login-card"><LockKeyhole/><p className="eyebrow">Restricted archive</p><h1>Administrator access</h1><p>Authorised custodians only. All attempts are logged, including the imaginative ones.</p><div><Label htmlFor="email">Email address</Label><Input id="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></div><div><Label htmlFor="password">Password</Label><Input id="password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></div>{message&&<p className="form-message" role="alert">{message}</p>} {!supabaseConfigured&&<p className="config-note">Supabase credentials are not configured on this deployment.</p>}<Button type="submit" disabled={busy}>{busy?'Verifying…':'Enter the archive'}</Button><Link href="/">Return to public archive</Link></form></main>}
