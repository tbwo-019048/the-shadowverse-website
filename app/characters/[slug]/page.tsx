import type{Metadata}from'next';import Link from'next/link';import{notFound}from'next/navigation';import{ArrowLeft}from'lucide-react';import{SiteHeader}from'@/components/site/site-header';import{SiteFooter}from'@/components/site/site-footer';import{characters}from'@/lib/content';
export async function generateStaticParams(){return characters.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=characters.find(x=>x.slug===slug);return{title:c?.name??'Dossier',description:c?.summary}}
export default async function CharacterPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=characters.find(x=>x.slug===slug);if(!c)notFound();return <main><SiteHeader/><section className="dossier-detail"><div className="shell dossier-detail-grid"><div><Link className="back-link" href="/characters"><ArrowLeft size={15}/> All dossiers</Link><div className="portrait-large" aria-label={`Portrait placeholder for ${c.name}`}><span>{c.initials}</span><small>Image withheld</small></div></div><article><p className="eyebrow">Dossier · {c.group}</p><h1>{c.name}</h1><p className="detail-deck">{c.summary}</p><blockquote>“{c.quote}”</blockquote><h2>Known history</h2><p>{c.bio}</p><dl className="detail-facts"><div><dt>Status</dt><dd>{c.status}</dd></div><div><dt>Classification</dt><dd>{c.group}</dd></div><div><dt>Species / type</dt><dd>{c.species}</dd></div><div><dt>Affiliation</dt><dd>{c.affiliation}</dd></div><div><dt>First appearance</dt><dd>{c.firstAppearance}</dd></div></dl></article></div></section><SiteFooter/></main>}
